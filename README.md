# learning_git

This repository was cloned to terminal using ssh mechanism

 1) ssh keygen -t rsa, this generates a ssh key
 2) The file will be stored in desired loction  of your of by default to Users/pavan/.ssh , this folder has the token
 3) Copy the token from the id file and paste it to the SSH in the github


This repository is used  to learn git advance concepts such as cheery-pick, rebase, merge etc 
